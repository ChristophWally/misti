# Per-repo Codex policy (with compatible keys across builds)
approval_policy = "on-request"      # preferred current key
approval_mode   = "on-request"      # fallback key used by some builds
approval        = "on-request"      # legacy fallback (ignored by others)

sandbox_mode    = "workspace-write" # already working
